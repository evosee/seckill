<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.example.demo.seckill.dao.OneDao">
    <insert id="save" parameterType="com.example.demo.seckill.bean.OneBean">
        insert into one(name) values(#{name})
    </insert>
</mapper>